services:
  - type: web
    name: smarthire
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: EMAIL_ENABLED
        value: "false"
